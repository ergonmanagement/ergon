/** Max optional user context length (chars). */
export const MARKETING_MAX_CONTEXT_LENGTH = 2000;

/** SMS body cap after generation (GSM-ish limit for v1). */
export const MARKETING_SMS_MAX_LENGTH = 160;

/** Default OpenAI model when MARKETING_OPENAI_MODEL is unset. */
export const MARKETING_DEFAULT_OPENAI_MODEL = "gpt-4.1-mini";
